package parser.enums;

public enum ArgumentPosition {
	/**
	 * Argument is in the declaration of an object
	 */
	DECLARATION,
	/**
	 * Argument is used in the call expression
	 */
	CALL_EXPRESSION;
}
